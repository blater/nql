package blater.nql.domain;

/** Stable resolver type retained for hierarchy mapping compatibility. */
final class HierarchyPathResolver extends HierarchyPathResolutionService {
  static class RowContext extends HierarchyPathResolutionService.RowContext {
  }
}
